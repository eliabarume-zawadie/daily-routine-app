# Tasks

<!--
DO NOT READ THE LINES BELOW AS TASKS. Everything inside this HTML comment is
format documentation for a human editor. It contains NO real to-dos. Every
example line is prefixed with "EXAMPLE (not a real task):" so that even a
naive line-by-line reader can tell it is not actionable.

Format: one to-do per line, written BELOW this comment block.
Optional priority tag at the start: [P1] [P2] [P3]  (P1 = highest)
Optional due date at the end: (due YYYY-MM-DD)

Format examples only — these are illustrations of the syntax, not work items,
and must never appear in a briefing:
EXAMPLE (not a real task): [P1] Finish Q3 budget review (due 2026-08-15)
EXAMPLE (not a real task): [P2] Reply to vendor contract email
EXAMPLE (not a real task): Read up on new onboarding docs

Real tasks go after the "-->" line below. This file currently has none.
-->
